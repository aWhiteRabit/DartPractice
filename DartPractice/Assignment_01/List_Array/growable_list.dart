void main(){
  var myGrowableList = [];
  print(myGrowableList);


  myGrowableList.add(45);
  print(myGrowableList);  //added item to the growable list;

  myGrowableList.add(45);
  print(myGrowableList);  //added item to the growable list;

  myGrowableList.add(45);
  print(myGrowableList);  //added item to the growable list;
}
